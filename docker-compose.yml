services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: atak-api
    command: python -m app.main
    ports:
      - "8002:8002"
    volumes:
      - ./app:/app/app:ro
      - ./certs:/app/certs:ro
    env_file:
      - .env.docker
    networks:
      - atak-network
    restart: unless-stopped

networks:
  atak-network:
    driver: bridge
